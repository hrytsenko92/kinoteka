import * as React from 'react';

type Props = {
  label: string;
  count: number; // ???
};

export const Slider: React.FC<Props> = props => {
  const { label, count } = props;

  return (
    <div>
    </div>
  );
};